# callback functions that users want us to analyze
callbackcfgs:
  - package: time
    method:
      - name: AfterFunc
        receiver: nil
        # tell me the make closure function name, pkg and receiver (if any)

  - package: sync
    method:
      - name: Range
        receiver: sync.Map
